doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title ANZOOM
        link(rel="stylesheet", href="https://unpkg.com/mvp.css")
        link(rel="stylesheet", href="css/index.css")       
    body 
        header 
            h1 ANZOOM
            ul#service_info 
                li 접속 인원:
                    span.user_count 0
                li 개설된 방의 수:
                    span.room_count 0
                li 당신의 이름:
                    span.username 익명
        main             
            div#welcome
                form.nickname_form
                    input(type="text", placeholder="닉네임을 입력해주세요.(선택사항)")
                    button(type="submit") 저장
                form.room_form
                    input(type="text", placeholder="방 이름을 입력해주세요.", required)
                    button(type="submit") 들어가기
            div#room                
                h2.room_name
                p 참가 인원:
                    span.room_user_count 0
                ul.chat_list
                form.message_form
                    input(type="text", placeholder="메시지를 입력해주세요.", required)
                    div
                        button(type="button").exit_button 나가기
                        button(type="submit") 전송    
        script(src="/socket.io/socket.io.js")
        script(src="js/index.js")